using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Used in combination with Gates (which take players to the next region.)
// A gate will only be open when all of its Seals are unlocked.
public class Seal : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
